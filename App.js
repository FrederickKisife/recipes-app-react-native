import React from 'react';
import { StyleSheet, Text, View } from 'react-native';
import AppContainer from './src/navigations/AppNavigation';

/*A container that runs the app */
export default function App() {
  return (
     <AppContainer />
  );
}
